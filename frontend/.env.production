# En producción (Vercel) NO usar URL absoluta para la API.
# Las peticiones van a /api/* que Vercel rewritea al backend.
# Así se evita Mixed Content (HTTP bloqueado desde HTTPS).
VITE_API_URL=

# URL de la PWA en Vercel
VITE_PWA_URL=https://repo-gps.vercel.app
